name: Bump version on main

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  bump:
    uses: .github/workflows/bump-version/action.yml@main
    with:
      default_branch: main
